fastapi==0.116.1
# Intel macOS is supported only through PyTorch 2.2.x (NumPy 1.x ABI).
numpy==1.26.4; sys_platform == "darwin" and platform_machine == "x86_64"
numpy==2.2.6; sys_platform != "darwin" or platform_machine != "x86_64"
python-multipart==0.0.20
torch==2.2.2; sys_platform == "darwin" and platform_machine == "x86_64"
torchaudio==2.2.2; sys_platform == "darwin" and platform_machine == "x86_64"
torch==2.8.0; sys_platform != "darwin" or platform_machine != "x86_64"
torchaudio==2.8.0; sys_platform != "darwin" or platform_machine != "x86_64"
uvicorn[standard]==0.35.0
